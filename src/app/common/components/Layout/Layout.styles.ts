import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
  root: {
    flexGrow: 1,
    maxWidth: '100%',
    overflow: 'hidden',
    height: 'auto',
    marginTop: -50,
    paddingTop: 40,
  },
}));
